
services:
  mongo:
    image: mongo:7
    container_name: hookrelay_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7-alpine    
    container_name: hookrelay_redis
    ports:
      - "6380:6379"         

volumes:
  mongo_data:       